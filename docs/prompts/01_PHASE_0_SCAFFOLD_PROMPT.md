# Claude Code Prompt — Phase 0 Scaffold

Read all docs in this folder first.

Create the monorepo scaffold for Bluegrass Gameday:

```text
apps/mobile_flutter
apps/admin_portal
functions
packages/shared_models
packages/stats_engine
firebase
scripts
seed_data
docs
```

Implement:
1. Flutter app skeleton with routing, theme, feature folders, and mock home screen.
2. React + Vite + TypeScript admin portal skeleton.
3. Firebase Functions TypeScript project.
4. Firebase emulator config.
5. Firestore rules and indexes starter.
6. Shared TypeScript/Dart model documentation.
7. Seed data loader script.
8. Root README with commands.

Do not connect real external APIs yet. Use mock data only.

Stop after Phase 0 and summarize files created, commands to run, and next recommended prompt.
