{
  "name": "bluegrass-gameday",
  "version": "0.1.0",
  "private": true,
  "description": "Bluegrass Gameday — independent Kentucky sports fan + stats app (monorepo root).",
  "scripts": {
    "emulators": "firebase emulators:start --import=./emulator-data --export-on-exit=./emulator-data",
    "emulators:fresh": "firebase emulators:start",
    "seed": "cross-env FIRESTORE_EMULATOR_HOST=localhost:8080 GCLOUD_PROJECT=bluegrass-gameday-dev npm --prefix scripts run seed",
    "deploy:rules": "firebase deploy --only firestore:rules,firestore:indexes,storage",
    "generate": "npm --prefix scripts run generate",
    "apply-articles": "npm --prefix scripts run apply-articles",
    "check-keys": "npm --prefix scripts run check-keys",
    "_comment_sync-data": "NODE_TLS_REJECT_UNAUTHORIZED=0 is required only because this dev machine sits behind a corporate SSL-inspection proxy that breaks Node's TLS to the CFBD/CBBD APIs. LOCAL DEV ONLY — do not use in CI/production.",
    "sync-data": "cross-env NODE_TLS_REJECT_UNAUTHORIZED=0 npm --prefix scripts run sync-data",
    "vault-sync": "cross-env NODE_TLS_REJECT_UNAUTHORIZED=0 npm --prefix scripts run vault-sync",
    "generate-legend": "cross-env NODE_TLS_REJECT_UNAUTHORIZED=0 npm --prefix scripts run generate-legend",
    "dev-claim": "cross-env FIREBASE_AUTH_EMULATOR_HOST=localhost:9099 GCLOUD_PROJECT=bluegrass-gameday-dev npm --prefix scripts run dev-claim"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
