# Local dev only — disables SSL verification for npm registry.
# Required when operating behind corporate SSL inspection proxy.
# Do not commit this file if your CI environment has valid SSL.
strict-ssl=false
